{% extends "./base.html"%}

{% block main%}
<div class="row">
    <div id="vmaig-content" class="col-md-8 col-lg-9">
        <div id="all-post-list" class="well">
            {% if article_list %}
            {% with post_list=article_list %}
            {% for post in post_list %} 
                {% include "./include/category_post.html"%}
            {% endfor %}
            {% endwith %}
            {% endif %}
        </div>
        
        <!--分页 -->
        {% if page_obj%}
        {% include "./include/pagination.html"%}
        {% endif %}
    </div>
    <div id="vmaig-side" class="col-md-4 col-lg-3 hidden-xs">
        {% include "./widgets/tags_cloud.html"%}
        {% include "./widgets/search.html"%}
        {% include "./widgets/wechat.html"%}
        {% include "./widgets/cloud.html"%}
        {% include "./widgets/hotest_posts.html"%}
        {% include "../vmaig_comments/latest_comments.html"%}
        {% include "./widgets/links.html"%}
    </div>
</div>
{% endblock %}
